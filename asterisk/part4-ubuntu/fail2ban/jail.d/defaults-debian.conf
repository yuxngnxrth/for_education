# Перекрывает /etc/fail2ban/jail.d/defaults-debian.conf из пакета Debian.
# Debian включает [sshd] с logpath=/var/log/auth.log, которого нет на хостах
# с journald-only. Отключаем его здесь; вместо него — наш [ssh] jail (ssh.conf).
[sshd]
enabled = false
